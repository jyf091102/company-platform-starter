import{api,session,setStatus}from'./app.js';const forms={login:document.querySelector('#loginForm'),register:document.querySelector('#registerForm')};document.querySelectorAll('[data-tab]').forEach(button=>button.addEventListener('click',()=>{for(const[name,form]of Object.entries(forms))form.hidden=name!==button.dataset.tab;document.querySelectorAll('[data-tab]').forEach(item=>item.classList.toggle('active',item===button))}));for(const[type,form]of Object.entries(forms))form.addEventListener('submit',async event=>{event.preventDefault();const status=form.querySelector('.status');setStatus(status,'正在处理…');try{const data=await api('/auth/'+type,{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(form)))});if(data.session)session.set(data.session);setStatus(status,data.message||'操作成功');if(type==='login')location.href='portal.html'}catch(error){setStatus(status,error.message,true)}});

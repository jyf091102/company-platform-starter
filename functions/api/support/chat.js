import{body,clean,json}from'../_shared.js';
const knowledge=[['项目','你可以在业务协作门户查看项目状态、最近更新和服务请求。'],['报价','报价模块用于展示待确认方案；正式接受流程应由采用者按业务规则实现。'],['资料','客户资料页只收集企业背景、服务需求和沟通偏好。'],['邮箱','企业邮箱页面提供集成边界和上游开源项目说明。']];
export async function onRequestPost({request,env}){let input;try{input=await body(request,6000)}catch{return json({error:'Invalid request'},400)}const message=clean(input.message,1000);if(!message)return json({error:'Message is required'},400);const found=knowledge.find(([key])=>message.includes(key));if(env.AI){const result=await env.AI.run(env.AI_MODEL||'@cf/meta/llama-3.1-8b-instruct-fast',{messages:[{role:'system',content:'You are support for a generic small-business portal. Answer briefly. Never request passwords, identity numbers, payment data, or claim an action was completed.'},{role:'user',content:message}],max_tokens:220});return json({reply:result.response||'请联系人工客服。',mode:'ai'})}return json({reply:found?.[1]||'这是客服骨架的演示回复。若问题涉及账户、订单或具体业务，请选择人工转接。',mode:'fallback'})}

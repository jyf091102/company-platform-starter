import{api,setStatus}from'./app.js';const form=document.querySelector('#profileForm'),status=form.querySelector('.status');form.addEventListener('submit',async event=>{event.preventDefault();setStatus(status,'正在保存…');try{await api('/customer-profile',{method:'PUT',body:JSON.stringify(Object.fromEntries(new FormData(form)))});setStatus(status,'资料已保存。')}catch(error){setStatus(status,error.message,true)}});
